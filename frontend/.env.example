# Set this to your deployed backend URL (must be https in production)
# Example: https://nova-backend.onrender.com
VITE_API_BASE_URL=

# Optional: enable Google OAuth via Supabase (if omitted, email/password auth still works)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
